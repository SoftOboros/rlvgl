[package]
name = "rlvgl-sim"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rlvgl= { version = "0.1.4", path = "../..", features = ["simulator", "qrcode"], default-features = false }
embedded-graphics = "0.8"

