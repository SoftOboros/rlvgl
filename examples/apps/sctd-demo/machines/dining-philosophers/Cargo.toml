[package]
name = "dining-philosophers"
version = "0.1.0"
edition = "2021"
description = "Generated SCXML state machine crate (Dining Philosophers tutorial)"
license = "BSD-1-Clause"

# Provenance:
#   Source:    vendor/scjson/tutorial/Examples/StateCharts/DiningPhilosphersProblem/
#              machine_dining_philosphers.flat.scxml
#   Generator: Softoboros iState MCP codegen (istate_codegen_create / rust_ir_emitter)
#   Date:      2026-06-20
#   Original:  /tmp/dp_crate/ (iState-MCP-generated Rust bundle)
# This crate MUST NOT be edited by hand; regenerate via the iState MCP pipeline.

[lib]
name = "dining_philosophers"
path = "src/lib.rs"

[features]
default = ["std"]
std = []
no_std = []

[dev-dependencies]

# NOTE: The [workspace] stanza is intentionally absent here because this crate
# is a path dependency of examples/apps/sctd-demo (a workspace member). Cargo
# cannot have a nested workspace root inside a workspace member's path dep tree.
# To run vector tests standalone (not via the parent workspace):
#   cargo test --manifest-path \
#     examples/apps/sctd-demo/machines/dining-philosophers/Cargo.toml
# will use the parent workspace, which is fine for testing purposes.
# Per SCTD-00 §9.4 (workspace isolation): the vector tests are directly
# runnable; workspace-wide clippy exclusion is handled by the workspace
# `exclude` list in the root Cargo.toml.
