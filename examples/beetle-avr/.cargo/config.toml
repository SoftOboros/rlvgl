[build]
target = "avr-none"
rustflags = ["-C", "target-cpu=atmega328p"]

[target.'cfg(target_arch = "avr")']
runner = "ravedude uno"
# For simulator runs:
# runner = "simavr -m atmega328p"

[unstable]
build-std = ["core"]
