//! Board support package for {{ ir.board.name }}.
//!
//! Auto-generated by `rlvgl-creator bsp from-yaml --vendor microchip`. Do
//! not edit by hand. Regenerate from
//! `chipdb/rlvgl-chips-microchip/db/chips/{{ ir.chip.name }}.yaml` and
//! `chipdb/rlvgl-chips-microchip/db/boards/{{ board_stem }}.yaml`.
//!
//! - Chip:    {{ ir.chip.name }} ({{ ir.chip.package }}, {{ ir.chip.arch }})
//! - PAC:     `{{ ir.chip.pac_crate }}` v{{ ir.chip.pac_version }}
//! - Flash:   {{ ir.board.flash_mb }} MB on-chip
//! - Source:  {{ ir.chip.source.datasheet }} rev {{ ir.chip.source.revision }}

#![no_std]
#![deny(missing_docs)]

pub mod board;
pub mod clocks;
pub mod io_mux;
pub mod pac;
pub mod peripherals;

pub use pac::init;
