# Platform Font Assets

This directory contains font files embedded by the `rlvgl-platform` crate when
font-backed rendering features are enabled. Keeping these files under the
package root ensures `cargo package` includes them for crates.io builds.
