// QT-08 fixture #2 of 2 — second QML in the same dir to verify
// the walker emits multiple per-basename outputs in lexical order.

import QtQuick 2.15

Item {
    id: b
    width: 200
    height: 200
}
