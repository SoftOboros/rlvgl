// QT-08 fixture #1 of 2 — exercises directory-mode walking.
// Kept tight; the per-file emit shape is already covered by
// other fixtures.

import QtQuick 2.15

Item {
    id: a
    width: 100
    height: 100
}
