{
  "name": "@rlvgl/audio-meters-core",
  "version": "0.2.0",
  "description": "L0 ballistic state machines and dB calibration helpers for rlvgl audio meters. TypeScript companion to rlvgl-audio-meters-core (Rust). Hand-ported; cross-runtime parity is verified via shared fixtures.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types --no-warnings test/parity.test.ts test/scales.test.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "files": [
    "src/**/*.ts",
    "README.md"
  ]
}
