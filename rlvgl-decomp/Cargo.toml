[package]
name = "rlvgl-decomp"
version = "0.2.3"
edition = "2021"
license = "MIT"
description = "Core compressed image (RLE) codec for rlvgl: mirror of example splashscreen RLE with RGBA pipeline."
readme = "README.md"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
