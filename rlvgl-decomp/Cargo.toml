[package]
name = "rlvgl-decomp"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Core compressed image (RLE) codec for rlvgl: mirror of example splashscreen RLE with RGBA pipeline."
readme = "README.md"
repository = "https://github.com/softoboros/rlvgl"
homepage = "https://github.com/softoboros/rlvgl"
documentation = "https://docs.rs/rlvgl-decomp"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
